import React from 'react';
import BaseLogin from './BaseLogin';

const LoginHousehold = ({ navigate }) => {
    return <BaseLogin navigate={navigate} type="Household" />;
};

export default LoginHousehold;
