import React from 'react';
import BaseLogin from './BaseLogin';

const LoginCompany = ({ navigate }) => {
    return <BaseLogin navigate={navigate} type="Company" />;
};

export default LoginCompany;
